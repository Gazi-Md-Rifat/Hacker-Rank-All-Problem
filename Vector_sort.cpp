#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> element(n);
    cout << element.size();
    // for (int i = 0; i < element.size(); i++)
    // {
    //     int x;
    //     cin >> x;
    //     element.push_back(x);
    // }
    // sort(element.begin(), element.end());
    // for (int i = 0; i < element.size(); i++)
    // {
    //     cout << element[i] << " ";
    // }

    return 0;
}
